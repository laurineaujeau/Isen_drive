extends layout

block content
    hr
    form(method='POST' action='')
        div.form-group
            label(for='name') Nom:
            input#name.form-control(type='text' name='name' required='true' value=category)

        button.btn.btn-primary(type='submit') Valider